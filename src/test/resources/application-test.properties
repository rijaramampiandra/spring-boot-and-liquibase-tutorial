# Override datasource for production.
spring.datasource.jdbcUrl=jdbc:h2:mem:H2DB_UNIT_TESTING;DB_CLOSE_DELAY=-1;MODE=MSSQLServer;DB_CLOSE_ON_EXIT=FALSE

# spring profile for TEST.
spring.profiles.active=test